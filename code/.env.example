# =============================================================================
# TOKENIZER42 - Configuration d'environnement
# =============================================================================
#
# INSTRUCTIONS DE SECURITE:
# -------------------------
# 1. Copiez ce fichier vers .env: cp .env.example .env
# 2. NE JAMAIS commit le fichier .env dans git
# 3. Utilisez des clés différentes pour testnet et mainnet
# 4. Stockez vos clés de production dans un gestionnaire de secrets
#
# =============================================================================

# -----------------------------------------------------------------------------
# CONFIGURATION OBLIGATOIRE
# -----------------------------------------------------------------------------

# Clé privée du wallet de déploiement (SANS le préfixe 0x)
# Exemple: abc123def456... (64 caractères hexadécimaux)
# ATTENTION: Cette clé a le contrôle total sur les contrats déployés
PRIVATE_KEY=your_private_key_here

# API Key BSCScan pour la vérification des contrats
# Obtenir une clé: https://bscscan.com/apis
BSCSCAN_API_KEY=your_bscscan_api_key_here

# -----------------------------------------------------------------------------
# CONFIGURATION RPC (optionnel - des valeurs par défaut sont fournies)
# -----------------------------------------------------------------------------

# URL RPC pour BSC Testnet (par défaut: RPC public Binance)
BSC_TESTNET_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545/

# URL RPC pour BSC Mainnet (par défaut: RPC public Binance)
# Pour la production, utilisez un RPC privé (Ankr, QuickNode, etc.)
BSC_MAINNET_RPC_URL=https://bsc-dataseed1.binance.org/

# -----------------------------------------------------------------------------
# CONFIGURATION OPTIONNELLE
# -----------------------------------------------------------------------------

# Activer le rapport de consommation de gas
REPORT_GAS=true

# Adresse du portefeuille pour recevoir les frais (si applicable)
# TREASURY_ADDRESS=0x...

# Nombre de confirmations à attendre avant de considérer une tx comme finale
# CONFIRMATION_BLOCKS=3

# =============================================================================
# BONNES PRATIQUES DE SECURITE
# =============================================================================
#
# 1. NEVER expose your private key:
#    - Don't commit .env to version control
#    - Don't share in chat/email/tickets
#    - Don't hardcode in source files
#
# 2. Use dedicated deployment wallets:
#    - Create separate wallets for testnet/mainnet
#    - Only fund with necessary amounts
#    - Consider using hardware wallets for mainnet
#
# 3. For production deployments:
#    - Use environment variables from CI/CD secrets
#    - Consider multi-sig for contract ownership
#    - Enable 2FA on all related accounts
#
# 4. Backup strategy:
#    - Keep encrypted backups of deployment wallets
#    - Document contract addresses and ownership
#    - Store recovery phrases securely offline
#
# =============================================================================
