# =============================================================================
# TOKENIZER42 - Environment Configuration Template
# =============================================================================
#
# SECURITY INSTRUCTIONS:
# ----------------------
# 1. Copy this file to .env: cp .env.example .env
# 2. NEVER commit the .env file to git
# 3. Use different keys for testnet and mainnet
# 4. Store production keys in a secret manager (Vault, AWS Secrets, etc.)
#
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED CONFIGURATION
# -----------------------------------------------------------------------------

# Deployment wallet private key (WITHOUT 0x prefix)
# Format: 64 hexadecimal characters
# WARNING: This key has full control over deployed contracts
PRIVATE_KEY=

# BSCScan API Key for contract verification
# Get a key at: https://bscscan.com/apis
BSCSCAN_API_KEY=

# -----------------------------------------------------------------------------
# RPC CONFIGURATION (optional - defaults provided by hardhat.config.ts)
# -----------------------------------------------------------------------------

# BSC Testnet RPC URL (leave empty to use default public endpoint)
BSC_TESTNET_RPC_URL=

# BSC Mainnet RPC URL (leave empty to use default public endpoint)
# For production: use a private RPC provider (Ankr, QuickNode, Alchemy, etc.)
BSC_MAINNET_RPC_URL=

# -----------------------------------------------------------------------------
# OPTIONAL CONFIGURATION
# -----------------------------------------------------------------------------

# Enable gas consumption report during tests
REPORT_GAS=true

# Treasury wallet address for fee collection (if applicable)
# TREASURY_ADDRESS=

# Number of confirmations to wait before considering a transaction final
# CONFIRMATION_BLOCKS=3

# =============================================================================
# SECURITY BEST PRACTICES
# =============================================================================
#
# 1. NEVER expose your private key:
#    - Don't commit .env to version control
#    - Don't share in chat/email/tickets
#    - Don't hardcode in source files
#    - Don't log private keys or sensitive data
#
# 2. Use dedicated deployment wallets:
#    - Create separate wallets for testnet/mainnet
#    - Only fund with necessary amounts
#    - Consider using hardware wallets for mainnet
#    - Revoke permissions after deployment if possible
#
# 3. For production deployments:
#    - Use environment variables from CI/CD secrets
#    - Consider multi-sig for contract ownership
#    - Enable 2FA on all related accounts
#    - Use a private RPC endpoint
#
# 4. Backup strategy:
#    - Keep encrypted backups of deployment wallets
#    - Document contract addresses and ownership
#    - Store recovery phrases securely offline
#    - Test recovery procedures regularly
#
# 5. Monitoring:
#    - Set up alerts for contract events
#    - Monitor deployer wallet activity
#    - Use services like Tenderly or OpenZeppelin Defender
#
# =============================================================================
